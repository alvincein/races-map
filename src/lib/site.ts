// Canonical site origin. Single source of truth for canonical URLs, the
// sitemap, robots.txt, Open Graph tags, and structured data.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || 'https://racemap.gr';
